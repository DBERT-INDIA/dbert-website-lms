# P2-7: Pinned dependencies for reproducible builds
flask==3.1.3
werkzeug==3.1.8
gunicorn==26.0.0
python-dotenv==1.2.2
requests==2.34.2
PyJWT==2.13.0
# Required only when EMAIL_PROVIDER=ses (Amazon SES HTTPS API):
boto3==1.43.56
# Used only by migrate_legacy_users.py / xlsx import (not required by app.py at runtime):
pandas==2.2.3
openpyxl==3.1.5
# HTML-to-PDF for the CV builder. Degrades to HTML if unavailable.
xhtml2pdf==0.2.17
# Fernet encryption for ambassador UPI ids
cryptography==50.0.0
